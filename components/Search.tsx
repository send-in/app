// #region imports
import {
	TextField, 
} from "@mui/material"
// #endregion

const Search = ({
	
}) => {
	return (
		<TextField
			// value={value}
			// onChange={handleChange}
			// placeholder={placeholder}
			size="small"
			variant="outlined"
			fullWidth
		/>
	)
}

export default Search